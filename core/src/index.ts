// App-facing surface: types + safe constants only. The real form configs are
// Node-only and reached via "@gar/core/forms" (never bundled into the app).
export * from './types';
export * from './constants';
